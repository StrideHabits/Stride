<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".core.LocalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StrideHabits">

        <!-- Disable WorkManager auto-init by removing its Startup entry -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>


        <activity
            android:name=".ui.layout.central.roots.HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.StrideHabits" />
        <activity
            android:name=".ui.layout.startup.roots.AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.StrideHabits">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".core.services.StrideFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
    
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>

<!--

// Manifest merger failed : Attribute meta-data#androidx.work.WorkManagerInitializer@value value=(false) from AndroidManifest.xml:24:17-38
//    is also present at [androidx.work:work-runtime:2.11.0] AndroidManifest.xml:36:17-49 value=(androidx.startup).
  //  Suggestion: add 'tools:replace="android:value"' to <meta-data> element at AndroidManifest.xml:22:13-24:41 to override.
-->
